[package]
name = "sophos-profile"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Importer for Sophos Connect (.scx) and legacy Sophos/Cyberoam (.tgb) VPN profiles, and Sophos Connect provisioning files (.pro)"

[dependencies]
vpn-core = { path = "../vpn-core" }
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
